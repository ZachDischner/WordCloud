## Assuming g++ compiler environment
env = Environment(CC='g++', CCFLAGS = "-Wall -Wextra -g -pedantic -std=iso9899:1999 -std=c++11")
# if you have specific, nonstandard g++ installed you can do :
# env = Environment(CC='g++-4.9', CCFLAGS ...
env.Program('WordCloud.cpp')